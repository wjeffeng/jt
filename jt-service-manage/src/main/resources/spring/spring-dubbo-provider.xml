<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
	http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
	http://code.alibabatech.com/schema/dubbo 
	http://code.alibabatech.com/schema/dubbo/dubbo.xsd">


	<!-- 提供方应用信息，用于计算依赖关系 -->
	<dubbo:application name="jt-service-manage" />

	<!-- 使用multicast广播注册中心暴露服务地址 -->
	<!--<dubbo:registry address="multicast://224.5.6.7:1234"/> -->
	<!-- <dubbo:registry address="zookeeper://192.168.16.50:2181" /> -->
	<dubbo:registry address="zookeeper://localhost:2181" />

	<!-- 用dubbo协议在20880端口暴露服务 -->
	<dubbo:protocol name="dubbo" port="${dubbo.protocol.port.jt-service-manage}" />

	<!-- 声明需要暴露的服务接口 -->
	<dubbo:service interface="com.jt.facade.manage.service.ItemService"
		ref="itemService" />
	<dubbo:service interface="com.jt.facade.manage.service.ItemCatService"
		ref="itemCatService" />
	<!-- 具体的实现bean -->
	<!-- <bean id="demoService" class="com.jt.dubbox.service.impl.ProcessDataImpl" 
		/> -->
</beans>